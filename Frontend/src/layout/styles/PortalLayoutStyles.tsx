import type { SxProps, Theme } from '@mui/material';

export const PortalLayoutStyles: Record<string, SxProps<Theme>> = {
  ul: {
    display: 'flex',
    alignItems: 'center',
    margin: 0,
    padding: '0 16px',
    height: '100%',
  },
};
